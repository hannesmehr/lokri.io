# i18n Error-Konventionen

Developer-Konventionen für die laufende i18n-Innenschicht-Migration.

## Grundsatz

- Backend wirft **Error-Codes**, nicht freie user-facing Texte.
- API-Responses serialisieren strukturierte Fehler über `codedApiError()`.
- Die sichtbare `message` ist aktuell nur ein **Übergangs-Fallback**, damit noch nicht migrierte UI-Stellen während des Phase-2-Rollouts keine UX-Regression bekommen.

## Response-Format

Für migrierte Fehler gilt:

```json
{
  "error": "Deutscher Fallback-Text",
  "details": {
    "code": "session.accountDisabled",
    "message": "Deutscher Fallback-Text",
    "status": 403
  }
}
```

- `details.code` ist der primäre, stabile Schlüssel.
- `details.message` ist vorübergehend wortidentisch zum deutschen Locale-Text.
- `error` bleibt für Alt-UI und bestehende Call-Sites als String erhalten.

## Backend-Regel

- Neue non-admin Fehler immer strukturiert serialisieren, bevorzugt via `codedApiError()`.
- Wenn ein Fehler über `ApiAuthError` läuft, gilt dieselbe Regel: `code` ist primär, `message` ist nur Fallback.
- Freie, direkt sichtbare Backend-Texte in non-admin Pfaden vermeiden.

## Frontend-Regel

- Frontend mappt `details.code` auf `t('errors.api.{area}.{key}')`.
- Wenn `details.code` unbekannt ist oder die Stelle noch nicht migriert wurde, fällt die UI auf `details.message` bzw. `error` zurück.
- UI-Migrationen passieren bereichsweise in Phase 2; Backend und UI werden bewusst entkoppelt.

## Admin-Scope

- Admin-Scope bleibt deutsch.
- Gilt für `app/(admin)/*`, `app/api/admin/*`, `lib/admin/*` und bewusst admin-spezifische Guard-Messages.
- Hintergrund und Ausnahmen sind in [docs/I18N_AUDIT.md](/Users/hannes/Development/private/lokri.io/docs/I18N_AUDIT.md:1) dokumentiert.

## Neue Errors hinzufügen

1. Backend-Code definieren oder bestehenden stabilen Code verwenden.
2. Key in `messages/de.json` und `messages/en.json` anlegen.
3. Deutscher Fallback im Backend muss **wortidentisch** zum DE-Locale-Text sein.
4. Response strukturiert serialisieren.
5. Falls die UI den Code schon kennt: auf `details.code` mappen. Falls nicht: Fallback intakt lassen.

## Pending Cleanups

- Nach Phase-2-Abschluss: `message`-Fallback aus der Error-Response-Struktur entfernen, sobald alle UI-Stellen auf `details.code + t()` mappen. Grep: `TODO(i18n-rollout)`-Kommentare im Backend.
- Beim Team-Settings-Rollout: Legacy-Code `CREATE_DISABLED` auf Dot-Notation `team.createDisabled` harmonisieren. Aktuell bleibt er aus UI-Kompatibilität mit [app/(dashboard)/_account-switcher.tsx](/Users/hannes/Development/private/lokri.io/app/(dashboard)/_account-switcher.tsx:202) bestehen.
